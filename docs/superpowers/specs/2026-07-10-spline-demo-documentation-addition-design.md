# 三次样条 Demo 路径生成文档增补设计

**日期**：2026-07-10  
**状态**：已确认设计，待书面复核

## 目标

在现有科教报告和项目接手手册中，补充一段简短、工程导向的 Demo 路径生成说明。内容以当前项目真实代码为依据，重点说明如何由少量动作关键点生成可训练的平滑示教轨迹，并配套两张图帮助下一届成员理解。

本次增补不扩展 GMR、DMP 或 ProMP 的轨迹生成理论，也不改变任何算法、数据或仿真行为。

## 事实依据与表述边界

说明内容以 `src/dobot_bgmm_promp/scripts/generate_palletizing_demos.py` 为唯一实现依据：

- 一条 Demo 由 8 个动作关键点构成：HOME、取物点上方、PICK、抬升、放置点上方、PLACE、离开放置点、返回 HOME。
- 夹爪关键状态为 `[0, 0, 1, 1, 1, 0, 0, 0]`。
- 关键点相位在 `[0, 1]` 上等距排列。
- 使用边界条件为 `natural` 的三次样条分别插值位置和夹爪信号。
- 每条轨迹输出 150 个采样点，夹爪插值结果裁剪至 `[0, 1]`。
- `NOISE_XY = 0.02`、`NOISE_Z = 0.02` 用于对取放位置和高度施加随机扰动，从同一动作结构生成多条 Demo。
- 生成结果是合成示教数据，不是实机拖动示教记录；路径也没有经过完整机械臂逆解、碰撞检测或物理抓取验证。

文档只说明三次样条在本项目中的作用：让相邻动作阶段之间连续过渡，相比直接用折线连接关键点更适合作为平滑的演示路径。避免展开样条基函数推导、矩阵求解和高阶公式。

## Word 报告增补

在报告第 2 章“系统与实验设计”的 `2.3 合成示教轨迹与数据格式` 下新增三级标题：

`2.3.1 基于三次样条的 Demo 生成`

正文控制为约 2 至 4 段，按以下顺序展开：

1. 说明 Demo 生成是本项目的数据准备环节，并列出 8 个动作阶段。
2. 说明对取放点和高度加入小范围随机扰动，以生成动作语义一致但路径略有差异的多条 Demo。
3. 说明使用自然三次样条将 8 个关键点扩展为 150 个连续采样点，并简述其相对折线路径的平滑性优势。
4. 用简短伪代码概括“关键点定义 -> 扰动 -> 样条插值 -> 夹爪裁剪 -> CSV 输出”的流程。

本节引用两张新增图，并使用与现有报告一致的题注、字号、段距和居中规则。新增标题纳入 Word 的真实标题层级，随后更新目录和页码。

## Markdown 手册增补

在 `6.1 当前示教数据` 对 8 个关键阶段的现有说明之后，新增聚焦小节：

`### 6.2 基于三次样条生成 Demo 路径`

原“安全地试验数据生成器”及其后小节顺延编号。新增小节包括：

- 8 个关键点及夹爪状态的简表；
- 关键点扰动和自然三次样条插值的四步流程；
- 与真实代码一致的简短伪代码；
- `N_TIME_STEPS`、`NOISE_XY`、`NOISE_Z` 三个主要参数的作用和修改风险；
- 安全试验建议：始终先输出到临时目录，查看曲线和 CSV，再决定是否替换训练数据；
- 两张新增图及简洁图注。

手册面向首次接手项目的学生，因此优先解释“为什么这样生成”和“修改参数后检查什么”，不要求读者掌握样条数学推导。

## 图表设计

两份文档共享以下永久图片，统一放入 `docs/images/`：

1. `spline-demo-keypoints.png`
   - 展示 8 个关键点、关键点连线和自然三次样条平滑路径。
   - 关键点按动作顺序编号，并在图例或标注中对应 HOME、PICK、PLACE 等阶段。
   - 视觉重点是“稀疏关键点如何变成连续路径”，不表现为实机可达性验证。
2. `spline-demo-variants.png`
   - 使用项目当前随机扰动逻辑和固定随机种子生成多条 Demo 路径。
   - 同时标出基准取放位置或基准路径，使读者能看出扰动只造成小范围变化。
   - 图注明确这些差异来自 `NOISE_XY` 和 `NOISE_Z`，不是四种学习算法的输出对比。

最终图片必须由当前项目常量和代码行为重新生成；头脑风暴阶段 `.superpowers/` 中的预览图仅用于选择视觉方案，不作为正式交付物，也不提交到仓库。

## 伪代码范围

两份文档使用同一逻辑，但 Word 版本更精简：

```text
定义 8 个动作关键点和夹爪状态
对 PICK、PLACE 和抬升高度加入小范围随机扰动
在归一化相位上执行自然三次样条插值
将位置与裁剪后的夹爪值组合成 150 个采样点
输出 t, x, y, z, gripper CSV
```

伪代码不暗示实时在线规划，也不把合成 Demo 描述成机器人自主规划结果。

## 验证与验收

实施完成后需满足以下条件：

- 两张图由当前生成器的常量、关键点顺序和自然三次样条逻辑生成。
- Markdown 中的参数值、阶段名称、输出列和运行建议与代码一致。
- Word 报告仅新增本次约定的小节和图表，不破坏原有封面、表格、页眉页脚、标题层级和整体排版。
- Word 目录使用真实域更新，新标题及页码可见。
- Word 经 Microsoft Word 导出 PDF 后，使用项目提供的 PDF 渲染工具逐页检查；图表、题注、分页和中文字体无明显异常。
- 项目自动化测试仍全部通过。
- 不提交 `.superpowers/`、临时 PDF、渲染页、缓存或其他中间文件。

## 不在本次范围内

- 修改 Demo 生成算法或默认参数；
- 重新生成并替换现有训练数据；
- 重新训练四种模型或改变现有对比结论；
- 增加 B 样条、Bezier 曲线或其他插值方法对比；
- 增加完整机械臂逆运动学、避障、碰撞检测或实机实验；
- 大幅扩写 GMR、DMP、分段 DMP 或 ProMP 的路径生成理论。
